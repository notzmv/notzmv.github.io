# Pink Color by Default for High Contrast Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change the default color for `.ar` and `.perso-arabic` elements from gold (`#fbbf24`) to pink (`#f472b6`) across `pt/index.html`, `index.html`, and `pt/curso.html` for maximum contrast and readability on dark backgrounds.

**Files:**
- Modify: `pt/index.html`, `index.html`, `pt/curso.html`

---

### Task 1: Update Default `.ar` Color in `pt/index.html`

- [ ] **Step 1: Set `.ar, .perso-arabic` default color to `#f472b6` (pink)**

---

### Task 2: Update Default `.ar` Color in `index.html`

- [ ] **Step 1: Set `.ar, .perso-arabic` default color to `#f472b6` (pink)**

---

### Task 3: Update Default `.ar` Color in `pt/curso.html`

- [ ] **Step 1: Set `.ar` default color to `#f472b6` (pink)**

---

### Task 4: Build & Verification

- [ ] **Step 1: Run `python3 build.py`**
- [ ] **Step 2: Run Python verification script confirming `.ar` elements default to `#f472b6` (pink)**
- [ ] **Step 3: Commit to Git**

```bash
git add pt/index.html index.html pt/curso.html docs/superpowers/plans/2026-08-25-default-pink-color-plan.md
git commit -m "feat(css): set pink (#f472b6) as default color for Perso-Arabic text for improved contrast"
```
